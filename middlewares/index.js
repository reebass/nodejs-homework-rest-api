const validateBody = require("./validateBody");
const isValid = require("./isValideId");
const auth = require("./auth");
const upload = require("./upload");


module.exports = {
    validateBody,
    isValid,
    auth,
    upload,
}